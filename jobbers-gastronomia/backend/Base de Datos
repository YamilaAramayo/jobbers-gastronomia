-- Crear tabla de categorías
CREATE TABLE IF NOT EXISTS categorias (
    id INT AUTO_INCREMENT PRIMARY KEY,
    nombre VARCHAR(100) NOT NULL,
    icono VARCHAR(50) NOT NULL,
    cantidad_ofertas INT DEFAULT 0
);

-- Crear tabla de ofertas de trabajo
CREATE TABLE IF NOT EXISTS ofertas (
    id INT AUTO_INCREMENT PRIMARY KEY,
    titulo VARCHAR(150) NOT NULL,
    empresa VARCHAR(150) NOT NULL,
    ubicacion VARCHAR(150) NOT NULL,
    tipo_jornada VARCHAR(50) NOT NULL,
    turno VARCHAR(50) NOT NULL,
    urgente TINYINT(1) DEFAULT 0,
    salario VARCHAR(100) DEFAULT 'A convenir',
    categoria_id INT,
    FOREIGN KEY (categoria_id) REFERENCES categorias(id) ON DELETE SET NULL
);

-- Insertar categorías de prueba
INSERT INTO categorias (nombre, icono, cantidad_ofertas) VALUES
('Cocina', '🍳', 1243),
('Mozo / Salonero', '🍽️', 982),
('Barista', '☕', 567),
('Bartender', '🍹', 458),
('Cajero', '💵', 324),
('Delivery', '🛵', 1105);

-- Insertar ofertas de prueba
INSERT INTO ofertas (titulo, empresa, ubicacion, tipo_jornada, turno, urgente, salario, categoria_id) VALUES
('Cocinero/a', 'Casa Norte', 'Nueva Córdoba', 'Tiempo completo', 'Turno Noche', 1, 'A convenir', 1),
('Barista', 'Café Central', 'Güemes', 'Part-time', 'Turno Mañana', 0, 'A convenir', 3),
('Bartender', 'Barra Sur', 'Centro', 'Tiempo completo', 'Turno Noche', 0, 'A convenir', 4);